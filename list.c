#include<stdio.h>
#include<stdlib.h>
#include "list.h"
Nptr make_list(){
    Nptr temp = (Nptr)malloc(sizeof(Node));
    temp -> node_ptr = NULL;
    dist = 0;
    next = NULL;
    return temp;
}
void insert_ordered(Nptr head,void* data_node ,){
    Nptr temp = head;
    while(temp -> next != NULL){
        if(temp -> )
        temp = temp->next;
    }
}
void push(void*);
Nptr pop();
